[project]
name = "invoicetronic-mcp"
version = "0.1.0"
description = "MCP server for Invoicetronic e-invoicing API (Italian SDI)"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
